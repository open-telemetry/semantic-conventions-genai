[project]
name = "mem0-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "genai-reference-shared",
    # TODO(pin): swap to released mem0ai after mem0ai/mem0#6325 ships.
    "mem0ai[otel] @ git+https://github.com/mem0ai/mem0.git@refs/pull/6325/head",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false

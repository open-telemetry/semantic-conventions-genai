[project]
name = "mem0-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "genai-reference-shared",
    # TODO(pin): swap to released mem0ai after mem0ai/mem0#6325 ships.
    "mem0ai[otel] @ git+https://github.com/mem0ai/mem0.git@2e47d2b70a90b0d2047317a3e1419ce32b68b6d0",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false

[project]
name = "mistralai-reference-test"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "mistralai==2.5.2",
    "genai-reference-shared",
]

[tool.uv.sources]
genai-reference-shared = { path = "../../shared", editable = true }

[tool.uv]
package = false
override-dependencies = [
    "opentelemetry-semantic-conventions==0.64b0",
]

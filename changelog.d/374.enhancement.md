Add `gen_ai.client.inference.usage.detailed.*` token counters that let consumers aggregate token spend across operations, broken down by the new `gen_ai.token.modality` attribute and by token category (cached, cache-written, reasoning).

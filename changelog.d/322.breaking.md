Align `invoke_agent` and `execute_tool` semantic attributes across spans and duration metrics; remove `gen_ai.agent.version` from `invoke_agent` internal span; mark `execute_tool` sampling-relevant attributes
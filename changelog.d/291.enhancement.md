Add agent authorization observability attributes under `gen_ai.agent.*`: `public_key.algorithm` (with fail-closed verifier guidance), `capability`, and producer-emitted `trust`/`drift`/`scan` scores each paired with an opaque `.method` token so consumers can distinguish a scoring-method change from a real change in agent behaviour.

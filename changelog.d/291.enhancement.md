Add agent authorization observability attributes under `gen_ai.agent.*`: `public_key.algorithm`, `capability`, and producer-emitted `trust`/`drift`/`scan` signals, each score or verdict paired with an opaque `.method` token so consumers can distinguish a scoring-method change from a real change in agent behaviour. `scan.verdict` carries a well-known value set. The attributes are self-reported observations of a decision already made, and each note states that a consumer should not re-derive an authorization decision from a received value.

Add the opt-in `gen_ai.skills` attribute on the invoke agent internal span, listing the skills whose instructions are in the invocation's context and whether each one has been replaced by a summary. A skill's instructions stay in context after the load that put them there, so this describes the context of every later operation in the conversation, not only the one that loaded it.

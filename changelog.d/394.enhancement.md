Add a `realtime_inference` operation and the `gen_ai.realtime_inference.client` span to model per-generation server-side output in realtime, bidirectional streaming sessions (such as OpenAI Realtime and Gemini Live), together with `gen_ai.client.realtime_session.started`/`gen_ai.client.realtime_session.ended` events for session lifecycle, a `gen_ai.realtime_session.id` attribute to correlate the generations, user inputs, and lifecycle events that belong to the same realtime session, an optional `user_speech` operation and `gen_ai.user_speech.internal` span for the user's utterance, recorded only when the provider emits server-side voice-activity (VAD) events that mark speech start/stop, audio token usage (`gen_ai.usage.audio.input_tokens`/`gen_ai.usage.audio.output_tokens`), a dedicated `transcription` message part that carries the text transcript of spoken audio as a standalone part (so audio and transcription, which providers stream and complete on independent boundaries, are not forced to share one part), and guidance on representing realtime tool calls as `tool_call`/`tool_call_response` message parts (sibling versus child generation shapes) rather than a client-side `execute_tool` span. Reference scenarios cover OpenAI Realtime and Gemini Live (sibling pattern) and Gemini Live 3.x (child pattern).

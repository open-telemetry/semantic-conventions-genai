Add a `realtime_inference` operation and the `gen_ai.realtime_inference.client` span to model per-generation server-side output in live, bidirectional streaming sessions (such as OpenAI Realtime and Gemini Live), together with `gen_ai.client.live_session.started`/`gen_ai.client.live_session.ended` events for session lifecycle, a `gen_ai.live_session.id` attribute to correlate the generations, user inputs, and lifecycle events that belong to the same live session, an optional VAD-gated `user_speech` operation and `gen_ai.user_speech.internal` span for the user's utterance, audio token usage (`gen_ai.usage.audio.input_tokens`/`gen_ai.usage.audio.output_tokens`), an optional `transcript` field on audio message parts to carry the text transcript of spoken audio, and guidance on sibling versus child tool-call span shapes. Reference scenarios cover OpenAI Realtime and Gemini Live (sibling pattern) and Gemini Live 3.x (child pattern).

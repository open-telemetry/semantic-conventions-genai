Clarify that the scope of `gen_ai.tool.definitions` depends on the signal it is reported on: the effective set of tools provided to a single model request on spans and events describing that request, and the set of tools configured for the agent on spans covering an agent invocation.

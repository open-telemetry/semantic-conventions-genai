Add a `generate_live_content` operation and the `gen_ai.generate_live_content.client` span to model per-generation server-side output in live, bidirectional speech-to-speech sessions (such as OpenAI Realtime and Gemini Live), together with `gen_ai.client.live_session.started`/`gen_ai.client.live_session.ended` events for session lifecycle, an optional VAD-gated `user_input` operation and `gen_ai.user_input.client` span for the user's utterance, and guidance on sibling versus child tool-call span shapes. Reference scenarios cover OpenAI Realtime and Gemini Live (sibling pattern) and Gemini Live 3.x (child pattern).

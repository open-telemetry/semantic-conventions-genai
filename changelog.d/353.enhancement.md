Add the `fetch_response` operation and `gen_ai.fetch_response.client` span for fetching a previously generated model response by its identifier without performing inference, with an OpenAI-specific refinement for the Responses API. The span also records `gen_ai.request.stream_cursor` when the fetch resumes a streamed response from a prior position, and `gen_ai.response.status` to capture the lifecycle status of the fetched response.

Add the `get_response` operation and `gen_ai.get_response.client` span for fetching a previously generated model response by its identifier without performing inference, with an OpenAI-specific refinement for the Responses API.

Fix reference `BlobPart` JSON encoding and decoding to use standard base64 for binary content, describe the encoding in the generated message schemas, and prevent invalid blob parts from using the generic input/output message fallback.

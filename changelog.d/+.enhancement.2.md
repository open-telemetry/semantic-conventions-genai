Add skill metrics: `gen_ai.skill.loads` and `gen_ai.skill.script.executions` count loads and script runs at the tool-execution boundary, while `gen_ai.invoke_agent.skill.loads` and `gen_ai.invoke_workflow.skill.loads` report how many distinct skills a single agent or workflow invocation activated. Adds `gen_ai.skill.script.exited_with_error` as the low-cardinality dimension for a script's own outcome.

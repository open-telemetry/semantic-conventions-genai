Add `run_step` operation, `gen_ai.step.name` attribute, `gen_ai.run_step.internal` span, and `gen_ai.run_step.duration` metric to capture units of work in a GenAI workflow that are not already covered by another GenAI span (e.g. agent transfer, tool call, LLM call, retrieval, memory operation), so that the workflow's execution graph can be reconstructed from telemetry. `run_step` MUST NOT be emitted for operations already instrumented by another GenAI span.

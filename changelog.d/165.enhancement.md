Add `agent.threat.detection.*` attributes and the `agent.threat.detection` event for recording the outcome of a runtime threat detection on an agent: the rule that fired, the ruleset it belongs to, the assigned severity, the enforcement action taken, and a correlation id linking the detection to the operation that triggered it.

Define tool-based agent transfer as the `gen_ai.execute_tool.transfer.internal` refinement of `gen_ai.execute_tool.internal`, and add the `gen_ai.invoke_agent.caller.client` refinement with `gen_ai.caller.type` and `gen_ai.caller.name` for protocol-based remote invocations whose immediate logical caller is explicitly available.

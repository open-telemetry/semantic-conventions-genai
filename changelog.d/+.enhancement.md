Update `mcp.*` attributes for the MCP 2026-07-28 protocol revision: bump the `mcp.protocol.version` example, add `mcp.method.name` well-known values for `server/discover`, `subscriptions/listen`, and `notifications/subscriptions/acknowledged` (the Multi Round-Trip Request flow introduced in that revision), and rewrite `mcp.session.id`'s description — the 2026-07-28 revision removes protocol-level sessions and the `initialize` handshake, so this attribute now only applies when interoperating with a pre-2026-07-28 peer via the backward-compatible `Mcp-Session-Id` header.

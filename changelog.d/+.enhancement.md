Update `mcp.*` attributes for the MCP 2026-07-28 protocol revision: bump the `mcp.protocol.version` example and `mcp.session.id` spec link, and add `mcp.method.name` well-known values for `server/discover`, `subscriptions/listen`, and `notifications/subscriptions/acknowledged` (the Multi Round-Trip Request flow introduced in that revision).

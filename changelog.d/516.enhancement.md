Record `gen_ai.usage.input_tokens` and `gen_ai.usage.output_tokens` on the `fetch_response` span when the fetched response carries usage, so a background execution polled with `fetch_response` is accountable from spans. Token usage metrics are still not reported for this operation.
